package saharaSolutionTest;

import saharaSolution.SaharaSolution;

import java.util.Objects;

import org.jetbrains.annotations.NotNull;
import org.jetbrains.annotations.Nullable;

import static outputUtilities.OutputUtilities.printLine;

//TODO: finish this and add javadoc
public class SaharaSolutionTest {

    /**
     * Main method of this test.
     * Run this to start.
     *
     * @param args Command-line arguments.
     */
    public static void main(String[] args) {
        //SaharaSolution solution = new SaharaSolution();
        //output(solution);
    }

    private static void output(@NotNull SaharaSolution solution) {
        //printLine("max height : " + solution.maxHeight);
        //printLine("height : " + solution.getHeight());
    }
}